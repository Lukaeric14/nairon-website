# Decision log

Format: date — decision — rationale. Locked decisions change only on Luka's explicit instruction.

## 2026-07-13 — Phase 0 interview (Luka)

1. **Verticals: three featured, four quiet.** Site/film show Solar, HVAC, Property Management; sales still serves Logistics, Franchises, Healthcare Staffing, Software. Rationale: focus follows existing traction; revenue outside the three is real.
2. **Site's job (12 months): sell consulting audits.** One conversion event. Platform shown as infrastructure. Rationale: consulting closes now; platform race needs the revenue and the case studies.
3. **Category term: "AI employees."** Word-of-mouth target sentence: "They built us an AI employee that does our permits. It's real work, not a chatbot."
4. **Only-we: both arms, one company.** Consultants who own outcomes + self-built supervision platform.
5. **Price posture: accessible premium.** Premium mechanics, no exclusivity signals. Rationale: land grab beats margin now; buyer must not be priced-scared or intimidated.
6. **Primary objection to design against: "too advanced for us."** Warm imagery dominates; every mechanism pairs with "we do this part."
7. **Voice: lawyer-grade, four absolute bans** (hype words, bot/copilot vocabulary, exclamation marks, unexplained tech terms). "Communicate like Legora. Like a lawyer." — Luka, verbatim.
8. **Anti-model: ServiceTitan.** Digestible, never dumbed down; modern, not mass-market.
9. **Full Hive stack diagram goes on the site.** Rationale: current site creates service-only misperception; the platform is the differentiator.
10. **Founder-led brand: Luka is the face.** Signed era-argument vision section; LinkedIn is a brand channel under the same voice doctrine.
11. **Hive stays industry-agnostic in all presentation.** Rationale: win state is the platform company, with other consultancies serving clients on Hive; vertical paint on the platform would need demolition later.

## 2026-07-13 — Phase 1 constraints (Luka)

12. **Gate 1 signed; STRATEGY.md locked.**
13. **Canvas: warm paper + dark cinematic hero.** Rationale: lowest intimidation against the #1 objection; the Legora structure.
14. **Kept assets: Nairon logo + Lapis blue trio.** Full type/palette system otherwise open.
15. **Imagery people rule: anonymized only, Luka excluded from site imagery.** Founder presence = signed vision section + LinkedIn (narrows decision #10's "appears in content").
16. **Imagery doctrine: the Tesla grammar.** Real warm environments + hardware as product photography; Legora close-up grammar applied authentically to the trades. Banned verbatim: "happy man on a jobsite checking things off his iPad." Source: Luka's three Tesla Solar screenshots, 2026-07-13.

17. **Accent resolved: "Lapis is it"** — kept as the single accent; the "overly blue" problem is fixed by the complementary environment (warm paper, tan wash, warm imagery), not a new accent. Copper demoted to detail-only, on probation until Gate 2. Amber-brown washes banned.
18. **Display type: B2, the lawyer-letter serif.** Licensed target Tiempos Headline; interim free stand-in Libre Baskerville. Body/UI stays Geist (Hive continuity); Geist Mono for data.
19. **DESIGN.md + STYLE-BIBLE.md drafted** (2026-07-13) — lock at Gate 2 together with the moodboard.

## 2026-07-13 — Moodboard round 1 verdict (Luka)

20. **Round 1 rejected as the moodboard; reclassified as film-scene candidates.** Verbatim: "i got scenes for the video perhaps in the contact sheets. but not website/brandkit level imagery." Frames stay banked for Phase 5.
21. **Two-register imagery doctrine.** Register A product-daylight (Tesla moodboard register) = default for website/brand kit; Register B cinematic dusk = film + accents. Both frozen blocks live in STYLE-BIBLE.md.
22. **The moodboard is assembled in Figma** in the Studio Outline template format (title serif top-left, off-white canvas, masonry grid, MOOD/FORM/TEXTURE/LIGHT footer) and must be solidified before any later phase moves. References: Tesla + Legora moodboard templates on ~/Desktop/legora-video/.
23. **Standing practice: strike through completed phases/steps in PROCESS.md's Progress section and the process artifact as work completes.**

24. **Moodboard focus: photography first (2026-07-13).** UI/product tiles are excluded from the current moodboard work; glassmorphic UI elements will be composed on top of photography later (the Legora structure). All photography is Higgsfield-generated (soul_cinematic, 2k, 0.12 credits/image), no stock.

25. **Generation lane locked to /hf-unlimited (Luka, 2026-07-13).** No CLI, no MCP generate calls, no paid generations. The one paid enhancer call (job 38986a32, ~3 credits) bought the reusable production-brief template now at `brand/moodboard/prompt-templates/`; template edits and new prompts are written by hand from that structure and run through the free web-UI lane.

## 2026-07-14/15 — Moodboard iteration verdicts (Luka, sequential, all binding — details per round in brand/images/LEDGER.md)

26. **Blue-led daylight grammar replaces sunset (v16, "why are we using the sunset theme instead of the blue?!").** FROZEN BLOCK A-BRAND-BLUE is the default brand block; sunset/amber banned from brand frames.
27. **Every brand frame carries a field-services subject (v17, "too abstract and not like its for brand identity").** Architecture is the stage, never the whole subject. PALETTE TEST from Luka's Frame 4: white/cream, near-black/charcoal, navy→lapis→sky→ice; orange/green = automatic fail.
28. **Model routing: soul_location at 0.12 is the lane for ALL brand frames.** soul_2 banned for scenes; soul_cinematic only for staffed silhouettes. Rationale: 0/7 usable (soul_cinematic) vs 8/8 (soul_location) on identical briefs; cost cap.
29. **Close-ups are object-scenes, not material swatches (v20 rejected in full: "this is all garbage").** One hero Nairon-world object pin-sharp in its real environment; bare texture crops dead.
30. **ANGLE RULE (v21: "pretty bad, try differetn angles, generate more").** Every object-scene names one deliberate camera position; angles vary across the set.
31. **Inside-looking-out frame-within-frame = confirmed keeper register (v22: "i like the van inside out").** Reused across subjects since.
32. **Territory multiplication approved (v23: "i really like these. go with a new batch")** — reflections-on-glass, scale-contrast, aerial, motion, knolling, inside-out extended through v24–v28 on the same recipe.
33. **VAN BAN + spiral register confirmed (2026-07-15, on v27/v28: "i like the spiral concept tbh. i dont want any more vans please").** No vans or fleet vehicles as subjects in any new frame (existing van keeps stay for curation). Spiral/radial/concentric geometry is KEEPER SIGNAL 2 — multiplied in v29 (atrium rings, tower spiral, radial courtyard delivered).
34. **REGISTER 3: property-manager office interiors (2026-07-15, Luka's reference image 213123.png: "now i need some more that are this concept. property management. office style.").** Anonymous manager from behind, slate-blue polo + grey trousers, keys in hand, bright black-framed-glass interiors with one warm material note. Keys-in-hand un-banned for this register (old ban covered still-life macros). Open question for Luka: his ref shows green trees through the glass — Frame 4 palette calls green an automatic fail; v30-door-keys delivered with the green flagged for his ruling.

35. **FIGURE REALISM RULE (2026-07-15, Luka on the full v34 round: "no these look really really bad. they dont look real...").** v34 (lit mid-size technicians walking through interiors) rejected wholesale. Root cause: soul_location composites mid-size, evenly-lit figures — they read as pasted cutouts. People appear ONLY as (a) true backlit near-black silhouettes with contact shadows or (b) tiny figures under ~10% of frame height in monumental frames. Spec'd in STYLE-BIBLE; v35 re-shot the register in these two treatments.

36. **FROZEN BLOCK A-BRAND-DOC — the documentary lane (2026-07-15, Luka on v35: "better but can be much better").** The residual fake-ness traced to A-BRAND-BLUE's own finish clauses ("commercial polish, hyper-realistic, no film grain") summoning render-perfect surfaces; the frames Luka rates real (v35-roof-distance) all disobeyed them. New verbatim block in STYLE-BIBLE: same poster/blue/anonymity DNA, finish swapped for documentary truth (analog film texture, named material wear, available light, "no CGI cleanliness"). DOC is the lane for people frames; A-BRAND-BLUE remains for unstaffed architecture/hardware. v36 shot on it: 5/6 concepts survived, the round reads photographic.

37. **REGISTER 4 — HAPPY WORK (2026-07-15, Luka on v36: "we need a happy vibe, not dark. happy construction works with a hint of the blue brand").** Dark/moody/noir grades are dead for brand frames. Worker imagery is bright high-key sunny daylight with blue as a HINT (sky band, navy workwear/case) never a wash; energy through motion and gesture readable in silhouette (high-five, mid-run, arm around shoulder) — the v32 family-energy lesson applied to workers. Spec'd in STYLE-BIBLE; v37 shot on it (5 keeps, ladder-hop the standout). Learning: mood adjectives on the figure ("happy spring") pull posed-stock faces — stage joy through gesture shapes, not adjectives.

38. **LIGHT-BLUE CALIBRATION of the DOC block (2026-07-16, Luka on v38/v39: "not as good to be honest. i think you are going too dark blue").** The dark drift traced to the block's own wording — "vast deep blue sky / frame held in cool blue" briefed navy-cobalt skies and ice-blue whites every roll. Block rewritten verbatim in STYLE-BIBLE: bright sun-filled frame, whites paper-white, blue only as a light accent (light summer-blue sky fading pale at the horizon), airy high-key exposure; "deep navy sky, cobalt, polarized sky, blue cast" joined every Avoid list. v40 re-staged the six best happy-work concepts on the corrected block: 6/6 landed light on first roll. The pre-calibration deep-blue wording is retired for all brand frames.

39. **BALANCE CALIBRATION of the grade (2026-07-16, Luka on v46–v48: "very white, need more blues, find a balance please").** The grade pendulum's final position after three verdicts (v38/39 "too dark blue" → v40 light calibration → v46–48 too white): whites bright but textured ("detail held in the whites"), sky a TRUE present summer blue — unmistakably blue, deepest at frame top, softening toward the horizon, never navy, never washed white — blue reinforced through navy wardrobe and glass reflections. Spec'd verbatim in the DOC block with guardrails against BOTH historical failure directions in every Avoid list. v49 verified 6/6 first-quality; v49-piggyback-blue / shoulders-blue / hall-race-blue are the reference frames for the target grade.

## 2026-07-23 — Hero-film rule review (Luka: "look at all your rules and strip the ones away that will hold us back from creating this video" → "now do the rule review and strip what blocks the video")

40. **FILM SCOPE established in STYLE-BIBLE.md: the hero video is a separate artifact from brand stills, with six rules stripped for film only.** Stripped for film (all remain binding for brand stills): (1) greenery suppression + residential-aerial ban — Scene 0 is a sunrise aerial over a California suburb, hills/lawns allowed, greens graded muted; (2) anonymity/no-faces — Register 6's faces exception extends to all film beats (Luka still never appears, #15 stands); (3) "smiling models" as applied to candid emotion — genuine smiles allowed (Scene 11 roofer), posed-stock grins and looking-at-camera stay banned; (4) vehicle avoids as world detail — incidental parked cars allowed/required for plausibility in street and aerial beats, vehicles never hero subjects, branded vans still banned; (5) hands-only close-up ban — soft hands beats are legitimate 1s breathing frames in film rhythm; (6) Frozen Block B as the film lane — superseded, the film runs on soul_location + Register 6 + the sunrise recipe; Block B survives for dusk accents only. Binding for film unchanged: text rule (all UI built, never generated), sunrise-not-sunset with pure soft blue sky, humble-father casting for home/commute beats, camera never upward, bare-headed, phone-screen grammar, plausibility two-pass, 0.12 cr stills discipline. Rationale: the stills rules were taste calibrations for brand-kit imagery; applied to the film they kill storyboarded beats outright (the Scene 0 aerial most directly). Open, deliberately not decided here: per-run cost approval for actual video-clip generation (CLAUDE.md cap), 16:9 vs 3:2 test at first film round, Scene 17 ending unwritten.

## 2026-07-24 — Hero film scenes 0–13 verdict pass (Luka, green dots on the storyboard + verbal walkthrough)

41. **Eleven scenes locked, three open; per-scene statuses live in `video/SHOT-LIST.md` (the film's spec artifact from now on).** Locked by green dot: S0 street-sun, S1 driveway-walk2, S2 sideangle-natural-check, S3 UI V1 working pill ("the first version"), S4 floor-walk, S5 lady-typing-b, S6 UI email V1, S7 gutter-check, S11 kneel-smile, S12 note-writing, S13 tenant-photo (dot green but verbally "okay for now" — improvable later). Open: **S8 REDO** (front of phone with a call happening, flat face-up on the toolkit, camera looking down, not on the roof — every prior variant showed the phone's back per the old phone-screen grammar; screen UI will be built+composited, not generated), **S9 REDESIGN** (doesn't show the AI answering — rebuild as the phone-shaped glass pane with an animated transcription of the AI taking the call and booking the job), **S10 REDESIGN** (same glass-pane format continues: call ends, job set up, ~$300 earned). **The Scene-06-V2 phone-shaped glass pane is a keeper motif** ("i love the glass planes that kind of look like a mobile") for reuse in later UI elements. Luka placed "Drawing of what i need" frames in columns 08/09/10 — drawings pending; Scene 8 generation waits for his drawing. Rationale: locks stop variant churn on solved scenes; the three UI-adjacent opens all trace to the same root — the beats must SHOW the AI working the phone, not imply it.

## 2026-07-24 — Scene 8/9/10 quality pass (Luka: "it should not be a UI element on top of the phone but rather the phone itself… the text is kind of like breaking apart… use like a more expensive model I'm okay with that")

42. **Expensive-model finishing lane approved and used; scene-8 text-rule carve-out.** (a) Scene 8's call UI is generated ON the phone screen via a nano_banana_pro image-edit of the approved soul_location plate, then 4k-upscaled — the single legal exception to "UI is never generated" (NBP renders exact text; every character verified in review; STYLE-BIBLE FILM SCOPE amended). (b) bytedance 4k upscale (2 cr flat) is the standard finishing step for picked frames/plates when definition matters; exploration stays at 0.12 cr Soul. (c) The scene-9/10 "text breaking apart" traced to my export chain (downscaling tile exports to 1920w) and the zoomed plate behind the type, not to the built vectors — fixed with the 4k plate upscale + full-resolution exports; tile assets now carry 2560w. This round's premium spend: ~6 cr (NBP edit + two 4k upscales, one failed 4k retried free of result).

## 2026-07-24 — Industry pages (Luka: "work all industry pages… almost exactly like this frame in paper called industry in inspiration page… nothing else")

43. **Industry pages built on the Legora industry-frame structure; industries consolidated to the real three.** (a) Page structure per Luka's spec, in order: nav, title block (eyebrow + headline + blue CTA pill), wide moodboard hero image, the THREE landing use-case sections verbatim (the choreographed UseCases component reused whole), an industry-specific FAQ, footer — nothing else. (b) The site previously carried 7 placeholder industries (commercial real estate, healthcare staffing, software & tech… no HVAC) in the nav, footer, and content collection — contradicting WhoItsFor/FAQ/meta which all say Solar, HVAC, Property Management. Replaced everywhere with the real three at /industries/solar, /industries/hvac, /industries/property-management; stale content files deleted. (c) Hero images are the already-approved industry card photos from the landing coverflow (solar rooftop install, HVAC heat-pump wall, PM keys-at-the-door) — no new/uncurated imagery introduced. (d) All headline/FAQ copy is reused approved landing copy (WhoItsFor descriptions as headlines; landing FAQ answers + use-case body copy as FAQ answers) — no new marketing prose invented. (e) Nav model extracted to `site/src/data/nav.ts` (shared with the concurrent nav rework); Nav.astro gained a `solid` prop for white-top subpages (starts and stays in its white/dark-text state). FAQ.astro parameterized (`faqs`/`sub` props, landing defaults unchanged).

## 2026-07-24 — Per-industry use cases (Luka: "chage the usecases to fit the industry… i need 3 use cases PER industry on their cards")

44. **Each industry page carries 3 industry-specific use-case cards — one of each landing panel type (chat, dispatch, table) — so the approved choreography transfers unchanged.** The landing card matching the industry is ported verbatim (solar permits / HVAC dispatch / PM messages); the six new cards fill the other two formats per industry: Solar = inspections-booked (dispatch) + chased-to-PTO (table); HVAC = same-day quotes (chat) + renewals (table); PM = leasing 24/7 (chat) + vendor dispatch (dispatch). Implementation: `IndustryUseCases.astro`, a parameterized copy of the landing component (landing `UseCases.astro` untouched — it stays pixel-locked to Paper GIK-0); content in `site/src/data/industryUseCases.ts`; panel status lines stamped as data attributes so each card narrates its own work on the same GSAP timelines. New-card photos and headshots come only from the already-shipped approved pool (`/paper/` + `/photos/`) — no uncurated imagery. WORKS-WITH logos = the industry's landing logo set on all three of its cards.

## 2026-07-25/26 — AI Academy brought on-domain + SEO/lead-magnet system (Luka: "take them and put them up on our website… 2 pages per youtube video… GREATly increase SEO content")

45. **The academy moves from `academy.naironai.com` to `naironai.com/academy/*`, and the standalone Vite/React SPA is retired in favour of Astro pages.** Rationale, in the order that decided it: (a) Google treats a subdomain as a separate site, so every link and ranking signal the academy earned was doing nothing for the main domain; (b) the SPA was client-rendered, so crawlers saw an empty shell — porting to Astro is what makes any of the content indexable, not a re-skin. Luka's call on scope: **full feature parity** (levels, topic hubs, prev/next, fuzzy search via fuse.js, localStorage progress + resume), and **retire the subdomain entirely** rather than 301 it — the backlink loss was flagged and accepted. Homepage academy cards and the footer link were re-pointed from YouTube/subdomain to on-domain lesson pages, so the homepage's authority now flows into `/academy/*`.

46. **Two pages per video, with the funnel page deliberately invisible to search.** `/academy/<slug>` is the indexable article; `/go/<magnet>` is the lead-magnet funnel, carrying `noindex, follow`, a canonical pointing at its lesson, and exclusion from the sitemap via an `astro.config.mjs` sitemap filter. Without this the two pages compete for the same query and Google picks one arbitrarily. Luka's original `/academy/c/<slug>` convention was renamed to `/go/<slug>`: it is the URL read aloud in a video and pasted into a description, so short and speakable beats namespace hygiene.

47. **Six shared lead magnets, not one per video (Luka's revision: "revolve everyone over 6 shared lead magnets").** A magnet per video means 43 assets to write, host, deliver and keep current, which no one has time for. Magnets are grouped by the reader's job, not by content topic: ai-fundamentals (6 lessons), ai-agents (13), ai-models (8), ai-cost (4), ai-prompting (3), ai-rollout (9). Mapping lives in `scripts/assign-magnets.mjs` so it is re-runnable rather than hand-maintained. **Open blocker: all six PDFs are placeholders** — the capture flow is real, the asset is a two-line stub. Nothing ships until Mahan supplies the real files.

48. **Article body is the markdown body; transcript is structured frontmatter.** Initially inverted (article in frontmatter, transcript in body). Flipping it means Astro's markdown pipeline renders the article for free with proper heading/table/blockquote handling, and the transcript becomes `{t, text}` data that can drive timestamp-seek buttons and `VideoObject` clip markers. Videos render as a click-to-load facade rather than a live iframe: a real embed costs ~600KB on every page load, which would gut Core Web Vitals on pages whose only job is to rank.

49. **43 articles written by a 122-agent swarm with an adversarial gate, not by a single pass (Luka: "1 agent per article… then adversary agent to get check for SEO and flag AI generated content").** Each writer got the transcript as its only permitted source of facts plus three house documents written first: `docs/ARTICLE-TEMPLATE.md` (shape), `docs/VOICE.md` (Luka and Mahan's actual speaking patterns, extracted from the transcripts rather than invented), and `docs/NO-SLOP.md` (anti-AI-slop rules distilled from the public catalogues — grahamrowe82/antislop, Rajat16nov/anti-slop, hardikpandya/stop-slop, slop-lint). Adversary scored slop and SEO separately and passed only at slop ≤3, SEO ≥7, zero hard bans; 38 of 42 failed and were revised. Result: 39,762 authored words, 0 em dashes, 0 hard-ban patterns, 126 internal cross-links (all resolving), 36 articles carrying a comparison table. `scripts/check-slop.mjs` makes the check repeatable and exits non-zero on hard bans; it exempts the `transcript` arrays deliberately, because people say "it's not X, it's Y" out loud and rewriting what someone said would be a lie.

50. **The swarm's real failure mode is cross-article repetition, and it needs a dedicated audit phase.** Forty-two independent agents converge on the same analogies: a 17-word sentence appeared verbatim in both the harness and orchestrator lessons, the same closing CTA in both Muse Spark articles, the France/capital analogy borrowed from the reference article. A final audit agent n-gram-diffed all 43 bodies and edited the weaker instance each time, dropping cross-article 7-gram overlap from 43 to 10 (survivors are shared model names and prices). It also caught a factual contradiction between three articles about which model led a leaderboard. Ten agents died on API 529s mid-run; a second repair workflow re-checked and fixed the 9 affected articles. Lesson for the next swarm: budget for a repair pass, and lock filenames explicitly in the brief (one writer renamed its own slug off-brief — kept, because `anthropic-fable-5-explained` genuinely beats the clickbait original for search).

## 2026-08-04 — Solar-only pivot (Luka: "i need to remove all HVAC, property management and all industries from our website. it needs to be purely solar now")

51. **Site repositioned from 3 industries (Solar/HVAC/Property Management) to solar-only, with 3 sub-niches inside solar: Installers & EPCs, Sales & Dealer Networks, O&M/Service.** (a) Spec-first: `brand/STRATEGY.md` section 3 (vertical canon) and `brand/DESIGN.md` (materials vocabulary) rewritten before any site code changed — HVAC and Property Management proof/canon fully removed, not just de-emphasized. (b) "Industries" dropped as a nav concept entirely (Luka's call, not just consolidated) — `/industries/*` route, its content collection, and `industryUseCases.ts` all deleted; `nav.ts` and `Footer.astro` lost the Industries dropdown/column. (c) `WhoItsFor.astro` (homepage coverflow) rebuilt around the 3 sub-niches — headline "Built for Solar," each card representing a stage of the solar business lifecycle (build / sell / maintain) rather than a vertical. (d) `UseCases.astro` (the landing page's pixel-locked 3-card showcase) kept card 1 (solar permits) verbatim and swapped cards 2–3 from HVAC dispatch / property-management table to solar content already built and shipped for the old `/industries/solar` page (inspections-booked dispatch, chased-to-PTO table) — reused existing approved content rather than inventing new design. (e) Meta/copy pass across Base.astro, about/careers/contact/how-we-work/404/academy-index/executive-ai-program — "field-services operator(s)" language replaced with solar-specific framing throughout, per Luka's explicit call to go fully solar rather than keep the broader umbrella term. (f) 27 academy articles + `docs/ARTICLE-TEMPLATE.md` doctrine + 6 lead-magnet pages scrubbed of HVAC/property-management worked examples, re-voiced to solar equivalents (sub-niche picked to fit the shape of the original example) by a dedicated subagent pass, verified against `docs/VOICE.md`/`docs/NO-SLOP.md` and `check-slop.mjs` (0 hard-ban hits). **Deliberately NOT edited:** two academy articles (`how-i-plan-to-make-200-videos-month-with-ai-agents.md`, `how-to-build-ai-employees-for-smbs.md`) state, as literal biographical fact from the source transcript, that Nairon has worked with solar/real-estate/property-management/logistics/marketing-agency clients historically — rewriting this to "solar only" would fabricate history and violate the "no invented facts" / "keep the hosts' actual claims" rules; Luka confirmed leaving these as-is. (g) Two new WhoItsFor hero photos generated for Sales & Dealers and O&M/Service via the `nairon-brand-imagery` skill (soul_location, 0.12cr lane, `brand/images/LEDGER.md` v103–v104) — Installers & EPCs kept its existing approved photo unchanged.
52. **Undocumented discrepancy found and now resolved:** decision #43 (2026-07-24) had already consolidated the site down to the real three industries (Solar/HVAC/PM) and deleted 7 stale placeholder verticals (commercial real estate, logistics/3PL, franchises, healthcare staffing, software/tech). Commit `8b1ab6e` later re-added all 5 of those placeholders back — with newly Higgsfield-generated imagery, real credits spent — without any DECISIONS.md entry explaining why, contradicting #43. This pivot removes them again, permanently, along with HVAC/PM. Flagging for the record in case the pattern recurs: a `git log` on `site/src/content/industries/` is the fastest way to catch this class of silent-scope-creep in future sessions.

## 2026-08-04 — Property-management-only re-pivot, same day (Luka: "change of strategy. We're going pure property management. So change everything that you did for solar and do the same thing for property management... leasing is one... dispatching of the crews... you can think of another one")

53. **Second vertical pivot in the same session: solar-only (#51) reversed and replaced with property-management-only, 3 sub-niches: Leasing, Maintenance & Dispatch, Rent & Renewals.** (a) The third sub-niche (Rent & Renewals) was Claude's proposal, not Luka's — reasoned from a clean MECE split of property-management operations (fill units / maintain them / keep the money and lease coming in), stated as a decision rather than asked as a question since it was low-risk (matches previously-approved pre-solar PM content) and Luka's instruction explicitly delegated the third pick ("you can think of another one"). (b) `brand/STRATEGY.md` and `brand/DESIGN.md` rewritten a second time; the flagship proof number reverted to the pre-solar original — "Property mgmt: 150 → 250 doors per manager (work-order triage + dispatch)" — since that number was never solar-specific to begin with, it was simply the wrong claim to have deleted during the solar pivot. (c) `WhoItsFor.astro` rebuilt again: Leasing reuses the pre-existing approved keys-in-lobby-with-rolled-plans photo (`/paper/00QY75TH63W9RRG9RP0QPEPTKS.webp`, unchanged since before the solar pivot); Maintenance & Dispatch and Rent & Renewals got new photos (`brand/images/LEDGER.md` v105–v106). (d) `UseCases.astro`: all three landing cards rewritten, not just two — row 1 had been solar-permits-flavored since before this session started (per decision #44, the landing page always showed one card per featured industry), so this is the first time it's carried property-management content. Card 1 (Leasing) and card 2 (Maintenance & Dispatch) reused verbatim copy recovered from `industryUseCases.ts` via `git show` on the pre-solar commit; card 3 (Rent & Renewals) is genuinely new copy — the old codebase never had a dedicated rent/renewals card, its table card blended leasing + rent + maintenance together, this round split rent/renewals out as its own clean pillar for the first time. (e) Sitewide copy pass and the 29-academy-article + 6-magnet re-scrub repeated a second time, this time explicitly instructed to re-voice FROM solar TO property management rather than revert to the old pre-solar HVAC-era wording — the subagent was told this was not a revert.
54. **Image generation quality note, logged for future rounds:** of 10 generations across v105–v106, only 3 were usable (30% hit rate, below this project's typical ~50%). Two new failure modes entered the doctrine this round: a "diorama/shadowbox" plausibility failure (a real-world doorway scene rendered as if displayed inside a floating museum shadowbox — fixed by explicitly stating "not a staged diorama... walls extend naturally... part of a continuous real building"), and a confirmed high-risk pattern where desk/paperwork scenes reliably render legible document or nameplate text despite explicit "no readable text" instructions on both the object and the Avoid list — three separate frames failed this way in one round. Recommendation for future desk/paperwork concepts: keep props closed/blank/at a comfortable middle distance, or avoid the genre in favor of an object/architecture register that doesn't require a legible-looking prop at all.

## 2026-08-05 — Favicon fix + SEO/performance audit (Luka: "our favicons and other meta data for the website is not set... check the old website repo and get the black logo to be the favicon")

55. **`favicon.ico` and `favicon.svg` were never Nairon-branded — both were the untouched Astro scaffold default (a gold/magenta abstract mountain icon), unrelated to the brand, shipped live on naironai.com since P0.** `apple-touch-icon.png` and `site.webmanifest` WERE correctly built for Nairon in commit `eb8cf31`, so only two of the five favicon surfaces were actually broken — not "everything." Root-caused by direct pixel inspection (rendered the live `.ico`/`.svg`, they showed a shape with zero relation to the "nairon." wordmark), not assumed from the bug report. Fix: pulled the proven, previously-shipped-in-production "n." monogram (dark-navy square, white glyph — the same lockup used for `apple-touch-icon.png` and the old `nairon-website` repo's favicon set) and generated `favicon-16x16.png` / `favicon-32x32.png` / a proper multi-resolution `favicon.ico` from it via Pillow; deleted the wrong `favicon.svg` rather than patch it (the old repo shipped no SVG favicon at all — ico+png+apple-touch-icon+manifest is the proven set, adding an svg back would be unvalidated surface area for no gain). Also fixed `site.webmanifest`'s stale `description` field ("AI workforce for field-services businesses" — pre-dated both vertical pivots) to match the current property-management copy, and added the 16/32 PNGs to its `icons` array. Verified: `astro build` clean at 74 pages, generated icons rendered legibly at 16/32/48px, `<head>` link tags confirmed present in build output. Not yet deployed to production — pending Luka's go-ahead (favicon changes are outward-facing on every page).
56. **SEO meta hygiene across all 74 pages is already strong — audited by scripting a scan of the full `dist/` build output, not sampling by eye.** Zero missing/duplicate titles, zero missing/duplicate descriptions, zero missing canonicals, zero multi-H1 pages, all `/go/*` funnels correctly `noindex` (per decision #46), rich structured data present sitewide (Organization/Article/VideoObject/FAQPage/Course/BreadcrumbList), unique OG images on 43/74 pages (the other 31 — legal/solutions/contact-type pages — fall back to `og-default.jpg`, which is fine, not a gap). No changes made here; this is a clean bill of health, logged so it isn't re-audited from scratch next time.
57. **Real performance finding, fixed same session (Luka: "both now").** (a) **Academy thumbnails**: all 43 shipped at the raw YouTube `maxresdefault` pull (1280×720, ~150-220KB each) into a 480×270 grid box (`LessonCard.astro`/`Academy.astro`) or a 56×32 search-result thumbnail (`AcademySearch.astro`) — up to 22x oversized in each dimension. Fix: added a `thumbUrlSmall()` helper (`academy.ts`) pointing at YouTube's own 480×360 `hqdefault` variant instead of a locally-resized copy — zero image-processing code needed, `object-fit:cover` already handled the aspect crop in both components. Backfilled all 43 existing thumbnails into `public/academy-thumbs-sm/` via direct fetch (0 failures); `scripts/ingest-video.mjs` now pulls both sizes for future videos. The full-res original stays untouched and in use for `LessonVideo.astro`'s hero + og:image/preload, which genuinely need it. Academy index page: 6.2MB → 684KB of thumbnail weight (89% cut), measured before/after on the actual build. (b) **Below-fold background-image → `<img loading="lazy">`**: converted only the substantial standalone content photos — `WhoItsFor.astro`'s 3 sub-niche cards and `UseCases.astro`'s 3 full-bleed panel photos (0D3A3QSMBE9DG5A0KFMJCXDMR8/uc-maintenance-dispatch/uc-rent-renewals) — to real `<img>` with descriptive `alt` text, native lazy-loading, and `object-fit:cover` matched to the prior `background-size:cover` box model. Deliberately left alone: `Section2.astro`'s deeply-nested pixel-perfect composited UI-mockup (dozens of tile fragments forming one glass-panel screenshot, precision-ported from Paper node IS8-0) and `UseCases.astro`'s small icon/avatar tiles — restructuring those into `<img>` risked visual regression in a pixel-locked component for marginal per-tile savings. Instead gave `Section2.astro`'s outer wrapper `content-visibility: auto` (verified safe against its own IntersectionObserver/gsap script — the observer targets the wrapper's own layout box, which content-visibility doesn't touch, only its descendants' paint), deferring its ~900KB of composited stills without touching a single line of the pixel-perfect markup. Did NOT add the same to `UseCases.astro`'s wrapper — its choreography script measures/animates descendants on scroll-in and `content-visibility` risks stale layout reads there; left as a flagged future candidate, not applied blind. `astro:assets`/`<Image>` is still unused anywhere in the codebase (every image is hand-written `<img src="/public/...">` at a fixed size) — worth a deliberate adoption decision later rather than continuing to patch image-by-image. Verified: `astro build` clean at 74 pages, all touched asset URLs confirmed 200 against a local preview server, markup spot-checked in build output. **Not screenshot-verified** — no browser-automation or headless-Chrome tool was available this session (checked and confirmed absent); CSS box-model equivalence was reasoned through property-by-property instead. Recommend a quick visual pass of the homepage (WhoItsFor carousel, UseCases rows) after deploy before calling this fully closed.
58. **Server/CDN layer already correct, confirmed by curling production, not assumed:** Brotli compression on HTML, `cache-control: public, max-age=31536000, immutable` on static assets, HTTP/2, HSTS — all Vercel defaults, all present. No action needed.

## Known issues — DO NOT FIX
- `legora-video-vibe-breakdown.md` stays in the repo root (reference material, not a stray file).
- Soft slop hits (~63 across 43 articles, about 1.5 each) are within tolerance. The
  anti-slop guidance is explicit that isolated tier-2 words and intensifiers read as
  human; only clusters are the tell. Do not chase these to zero — over-correction
  flattens the prose, which is the failure the rules exist to prevent.
- The five `ai-models-*` articles share a section template on purpose (it is a
  series). Verbatim sentences between them were removed; the shared shape stays.
