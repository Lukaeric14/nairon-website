{
	"$schema": "https://turbo.build/schema.json",
	"ui": "tui",
	"globalPassThroughEnv": [],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".vercel/output/**"],
			"env": ["VITE_CONVEX_URL", "VITE_CONVEX_SITE_URL", "FIRECRAWL_API_KEY"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"check-types": {
			"dependsOn": ["^build"]
		}
	}
}
