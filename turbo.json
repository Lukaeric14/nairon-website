{
	"$schema": "https://turbo.build/schema.json",
	"ui": "tui",
	"globalPassThroughEnv": ["CONVEX_DEPLOY_KEY"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".vercel/output/**"],
			"env": ["VITE_CONVEX_URL", "VITE_CONVEX_SITE_URL", "CONVEX_DEPLOY_KEY"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"check-types": {
			"dependsOn": ["^build"]
		}
	}
}
